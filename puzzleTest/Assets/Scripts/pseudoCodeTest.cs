using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pseudoCodeTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    /*
    1) Player mouse hovers over piece, then click, should be allowed to drag piece around.
    2) when dropped, piece checks for any collisions
    3) if collision found, checks side compatability
    4) if true, label a hit
    */
}
